# Portainer Stack 配置（简化版）
# 
# 直接复制此内容到 Portainer Stack Web Editor
# 
# 必须配置的环境变量：
#   - DB_PASSWORD: 数据库密码
#   - REDIS_HOST: Redis 主机地址
#
# 可选环境变量：
#   - APP_PORT: 应用端口（默认 3000）
#   - LOG_LEVEL: 日志级别（默认 info）
#   - ADMIN_PASSWORD: 管理员密码（默认 admin123）
#   - JWT_SECRET: JWT 密钥
#   - AUTH_KEY: API 认证密钥（默认 123456）
#
# 镜像地址：
#   - 腾讯云(香港): hkccr.ccs.tencentyun.com/gdgeek/log:latest
#   - GitHub: ghcr.io/gdgeek/hardware-log-system:latest

version: '3.8'

services:
  # 应用服务
  app:
    # 腾讯云镜像（推荐，国内速度快）
    image: hkccr.ccs.tencentyun.com/gdgeek/log:latest
    container_name: hardware-log-system
    ports:
      - "${APP_PORT:-3000}:3000"
    environment:
      # 数据库配置
      DB_HOST: mysql
      DB_PORT: "3306"
      DB_NAME: hardware_logs
      DB_USER: root
      DB_PASSWORD: ${DB_PASSWORD}
      
      # 日志配置
      LOG_LEVEL: ${LOG_LEVEL:-info}
      LOG_FILE: logs/app.log
      
      # API 配置
      API_PREFIX: /api/v1
      
      # 管理员认证
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-admin123}
      JWT_SECRET: ${JWT_SECRET:-hardware-log-system-secret-key-change-it}
      
      # API 认证
      AUTH_KEY: ${AUTH_KEY:-123456}
      
      # Redis 缓存 (腾讯云)
      REDIS_ENABLED: "true"
      REDIS_HOST: ${REDIS_HOST}
      REDIS_PORT: "6379"
      REDIS_PASSWORD: ""
      REDIS_TTL: "3600"
    depends_on:
      mysql:
        condition: service_healthy
    volumes:
      - app_logs:/app/logs
    networks:
      - app-network
    restart: always
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 5s
      start_period: 30s
      retries: 3

  # MySQL 数据库
  mysql:
    image: mysql:8.0
    container_name: hardware-log-mysql
    environment:
      - MYSQL_ROOT_PASSWORD=${DB_PASSWORD}
      - MYSQL_DATABASE=hardware_logs
      - MYSQL_CHARACTER_SET_SERVER=utf8mb4
      - MYSQL_COLLATION_SERVER=utf8mb4_unicode_ci
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - app-network
    restart: always
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    command: >
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
      --max_connections=200

networks:
  app-network:
    driver: bridge

volumes:
  mysql_data:
  app_logs:
